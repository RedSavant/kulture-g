FROM node:22-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci

FROM node:22-alpine
WORKDIR /app
COPY --from=build /app/node_modules ./node_modules
COPY backend ./backend
RUN mkdir -p backend-data pfp-data
EXPOSE 4000
ENV API_PORT=4000
CMD ["node", "backend/api-server.mjs"]
